{
  "buildCommand": "pnpm --workspace-root exec turbo --no-daemon --filter mfe1 build --env-mode loose",
  "installCommand": "node -v && pnpm --workspace-root --filter mfe1... install --config.dedupe-peer-dependents=false",
  "build": {
    "env": {
      "ENABLE_EXPERIMENTAL_COREPACK": "1"
    }
  }
}